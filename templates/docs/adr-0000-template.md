# ADR-0000: Title

Status: proposed
Affected paths: path/to/protected-scope, path/to/other-scope
Date: YYYY-MM-DD
Owners: team/name
Scope: modules/contracts/paths
Reviewers: architecture owner(s)
Supersedes: none

## Context

What problem and constraints require a decision?

## Decision

What is the chosen architecture and boundary?

State the scope explicitly. Name approved pattern and exemplar paths when future changes must follow them.

Keep every affected path or pattern on the single comma-separated `Affected paths:` line above. New Accepted ADRs and Proposed → Accepted ADRs are combined and must cover every changed protected path.

## Invariants and enforcement

Which rules remain true, and which tests, dependency checks, contracts or reviews enforce them?

## Consequences

What becomes easier, harder or newly required?

## Migration and rollback

How will existing data, contracts and deployments be migrated and reversed?

## Exceptions and expiry

List owner, reason, scope and expiry/review date for each exception. Write `none` when no exception is allowed.
